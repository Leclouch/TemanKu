<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- image_picker: camera capture for the guardian photo-upload flow (§5.1).
         Gallery picking uses the system Photo Picker on API 33+ and needs no
         storage permission; camera capture does. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- RECORD_AUDIO has two on-device-only consumers, both requested at
         speak-mode entry (not app launch), never at startup:
           - speech/silero_vad_service.dart — the default VadService. Detects
             *that and when* the child spoke; never records to a file, never
             transmitted anywhere.
           - speech/audio_segment_recorder.dart — the optional pronunciation-
             hint feature, which DOES capture and transmit a short clip, but
             only when a guardian has explicitly opted a child in
             (Child.pronunciationHintEnabled — features/guardian/child_settings_screen.dart's
             consent screen). Off by default.
         MODIFY_AUDIO_SETTINGS backs the voice-call-optimised audio routing
         `package:vad` requests by default (AndroidRecordConfig's
         audioManagerMode/speakerphone) for cleaner VAD input. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <application
        android:label="temanku"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
